# --- build cruft ---
node_modules/
package-lock.json
*.log

# --- test artifacts ---
test/shot-*.png

# --- level-build cache (regenerable; js/levels.js is the committed output) ---
scripts/.cache/

# --- design-process visual artifacts (kept on disk, out of the repo) ---
# design-brief.md is deliberately NOT committed — it stays on disk only.
# (Removed from the repo at Ruta's request; still present locally.)
design-*.html
accent-compare*.html
*-review.html

# --- OS / editor ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
.claude/
design-brief.md
